import Vue from "vue";
import Vuex from "vuex";
import rounds from "./modules/rounds";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: { rounds },
});
